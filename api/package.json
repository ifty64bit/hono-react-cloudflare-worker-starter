{
    "name": "api",
    "scripts": {
        "dev": "wrangler dev",
        "build": "wrangler deploy --dry-run",
        "deploy": "wrangler deploy --minify",
        "cf-typegen": "wrangler types --env-interface CloudflareBindings",
        "type-check": "echo 'Type checking not available for this package'",
        "lint": "echo 'Linting not configured for this package'"
    },
    "dependencies": {
        "hono": "^4.8.4"
    },
    "devDependencies": {
        "wrangler": "^4.4.0"
    }
}
